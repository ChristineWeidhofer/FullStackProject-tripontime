{% extends 'base.html.twig' %}

{% block title %}Edit Accommodation
{% endblock %}

{% block body %}
<div class="container">

	<h1>Edit Accommodation</h1>

	{{ include('accommodation/_form.html.twig', {'button_label': 'Update'}) }}
	<a class="btn btn-danger" href="{{ path('app_admin_index') }}">Back to my account</a>


	{{ include('accommodation/_delete_form.html.twig') }}
</div>

{% endblock %}
