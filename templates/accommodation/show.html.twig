{% extends 'base.html.twig' %}

{% block title %}Accomodation
{% endblock %}
{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset("css/about.css") }}">
	<link rel="stylesheet" href="{{ asset("css/admin.css") }}">

{% endblock %}

{% block body %}
	{% include "components/navbar.html" %}

	<div class="backColor w-75 mx-auto">
		<div class="container">
			<h1>{{ accommodation.name }}</h1>

			<div class="card-wrapper w-100 w-md-75 mx-auto ">
				<div class="card border-0 p-0 m-0d-flex flex-column flex-md-row">
					<div class="wrap-pic w-100 w-md-50">
						<img src="{{ accommodation.image }}" class="card-img-to w-100" alt="{{ accommodation.name }}" style="aspect-ratio:1;object-fit:cover">
					</div>
					<div class="wrap-body w-100 w-md-50">
						<div class="card-body">
							<h5 class="card-title">{{ accommodation.name }}</h5>
							<p class="card-text">{{ accommodation.description }}</p>
							<h5 class="card-title">Capactiy:</h5>
							<p class="card-text">{{ accommodation.capacity }}</p>
							<h5 class="card-title">Website:</h5>
							<p class="card-text">{{ accommodation.website }}</p>
							<h5 class="card-title">Rating:</h5>
							<p class="card-text">{{ accommodation.review }}</p>
						</div>
						<div class="card-body d-flex justify-content-center align-items-center">

							<a href="{% if is_granted('ROLE_ADMIN') %}{{ path('app_admin_index') }}{% else %}{{ path('app_user_profile') }}{% endif %}" class="btn btn-outline-dark rounded-0 mx-2">Back to my account</a>

							<a href="{{ path('app_accommodation_index') }}" class="btn btn-outline-dark rounded-0 mx-2">Back to list</a>
							<a href="{{ path('app_accommodation_edit', {'id': accommodation.id}) }}" class="btn btn-outline-dark rounded-0 mx-2">Edit</a>
							{% if is_granted('ROLE_ADMIN') %}
								{{ include('accommodation/_delete_form.html.twig') }}
							{% endif %}
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>


{% endblock %}
