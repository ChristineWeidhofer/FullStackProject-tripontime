{% extends 'base.html.twig' %}

{% block title %}Edit Restaurant
{% endblock %}

{% block body %}
<div class="container">

	<h1>Edit Restaurant</h1>

	{{ include('restaurant/_form.html.twig', {'button_label': 'Update'}) }}
	<a class="btn btn-danger" href="{{ path('app_admin_index') }}">Back to my account</a>
	{{ include('restaurant/_delete_form.html.twig') }}
</div>

{% endblock %}
