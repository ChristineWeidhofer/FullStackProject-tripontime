{% extends 'base.html.twig' %}

{% block title %}Packinglist{% endblock %}

{% block body %}
    <h1>Packinglist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ packinglist.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ packinglist.name }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ packinglist.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_packinglist_index') }}">back to list</a>

    <a href="{{ path('app_packinglist_edit', {'id': packinglist.id}) }}">edit</a>

    {{ include('packinglist/_delete_form.html.twig') }}
{% endblock %}
